Alhamulillah


# The Way to PGVector from minute 2:25

# >> nlp -> index project -> data batching [with progress-bar]
# >> implement total_chunk_count in chunk_model
# >> Solve async-await issues
# >> Lifespan compatible with the PG_Vector
# >> solve data bining issues [sql_text does not allow : with tables (not ordinary values) --> :collection_name]
# >> adding metadata as json to PGVector
# >> Revisit Chunking logic for more efficiency